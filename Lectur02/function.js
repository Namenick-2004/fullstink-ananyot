parameters (Input)
function findperimeter(length, width) {
    return 2 * (length + width);
}
process
const perimeter = (length, width) *2;
      return (output)

      let request =findperimeter(5, 10); // result is 30