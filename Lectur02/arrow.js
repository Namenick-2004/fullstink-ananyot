const a  = 6 ; 
const b = 10 ;
const add = function(x,y) {
    return x + y;
};
console.log('the sum of ' + a + ' and ' + b + ' is ' + add(a,b) );

const xadd = (x,y) => x + y;
console.log(`the sum of ${a} and ${b} is ${xadd(a,b) }`);

const square = x => x * x ;{
  return x - y 
}
// console.Log(`the difference of ${a} and ${b} is ${square(a,b) }`);      


